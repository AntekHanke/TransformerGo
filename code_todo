1. Typing
2. Remove unused code
3. Reformat code


Fix in modules:
- policy: remove random moves
- chess_engines: handle random moves in chess engines, debug


Modules cleaning:
TO: experiments, configures, local_runner (DONE), policy
GG: chess_engines, jobs,  assets



propes_best_moves(state, self.n_best-moves)

[m1, m2, m3]
for move in moves:
    if move in legal_moves:
        return move
else:
    if replave_
    return random.choice(legal_moves)
